{
  "name": "homebridge-irrigation-system",
  "displayName": "Homebridge Irrigation System",
  "version": "1.0.0",
  "description": "Homebridge plugin that creates a Heater Cooler system accessory that can be triggered by HomeKit sensors.",
  "main": "src/index.js",
  "scripts": {
    "test": "npm install -g && homebridge -I -D",
    "build": "exit 0"
  },
  "keywords": [
    "homebridge-plugin",
    "homebridge",
    "homekit",
    "irrigation"
  ],
  "engines": {
    "node": ">=10.x",
    "homebridge": ">=0.2.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-rate-limit": "^6.2.1",
    "node-fetch": "^2.6.7",
    "node-persist": "^3.0.5"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  },
  "publishConfig": {
   "@DomiBot:registry": "https://npm.pkg.github.com"
 }
}
